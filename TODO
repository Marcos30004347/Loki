Add += operator
Add -= operator
Add /= operator
Add *= operator
Add ++ operator
Add -- operator
Add || operator
Add && operator

Add more types...
Add support for funtion default arguments
Add verification to check if break is insile loop/switch statements