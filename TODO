Fix Problem: Functions can only return built in types

Add static
Add Export
Add Buffer
Add Uniform
Add macros support
Add support for funtion default arguments
Add verification to check if break is insile loop/switch statements

Add Better error report for user including lines and more.
Add Midle stage between parser and translation to check for errors

Fix the thousands memory leaks that the code already have
